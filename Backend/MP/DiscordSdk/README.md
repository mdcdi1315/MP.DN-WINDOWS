## Discord SDK for MusicPlayer.

From version 1.5.7.4 and on the app supports Discord Rich Presense with minimal
playback information on a Discord account.


The SDK is almost preserved as-is.

Some changes done is to improve the primary interop signature
and adapt the code so that it covers more fatal failures at startup.