

### The Interop namespace.

This namespace contains common and non-platform dependent code for enforcing several interoperability patterns during coding.

Typical developers should not use any types provided through this namespace, as all API's are subject to change without notice 
from the author because this changes as the app and it's needs are evolving.

